<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 自定义四角矩形ImageView 的四个圆角属性 -->
    <declare-styleable name="SRoundImageView">
        <attr name="left_top_radius" format="dimension" />
        <attr name="right_top_radius" format="dimension" />
        <attr name="left_bottom_radius" format="dimension" />
        <attr name="right_bottom_radius" format="dimension" />
        <attr name="radius" format="dimension" />
    </declare-styleable>

    <declare-styleable name="FlowLayout_LayoutParams">
        <attr name="layout_newLine" format="boolean" />
        <attr name="layout_weight" format="float" />
        <attr name="android:layout_gravity" />
    </declare-styleable>

    <declare-styleable name="FlowLayout">
        <attr name="android:orientation" />
        <attr name="layoutDirection" format="enum">
            <enum name="ltr" value="0" />
            <enum name="rtl" value="1" />
        </attr>
        <attr name="debugDraw" format="boolean" />
        <attr name="weightDefault" format="float" />
        <attr name="android:gravity" />
    </declare-styleable>

    <!-- 流布局-->
    <declare-styleable name="FlowLayout1">
        <!--支持折叠 -->
        <attr name="flow_fold_history" format="boolean" />
        <!--折叠时的行数（支持折叠情况下） -->
        <attr name="flow_foldLines_history" format="integer" />
        <!--平均分配 -->
        <attr name="flow_equally_history" format="boolean" />
        <!--平局分配一行的数量 -->
        <attr name="flow_equally_count_history" format="integer" />
        <!--水平间距 -->
        <attr name="flow_horizontalSpacing_history" format="dimension" />
        <!--竖直间距 -->
        <attr name="flow_verticalSpacing_history" format="dimension" />
        <!--左右对齐 -->
        <attr name="flow_gravity_history">
            <enum name="LEFT" value="0" />
            <enum name="RIGHT" value="1" />
        </attr>
    </declare-styleable>


</resources>